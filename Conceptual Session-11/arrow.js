/* function add(x, y) {
    const result = x + y;
    return result;
} */

// using arrow function:
/* const add = (x, y) => {
    const result = x + y;
    return result;
} */

// or,
// const add = (x, y) => x + y;
// have to wrute in a single line.
// can't use second bracket.

/* const show = () => console.log('hellow world');
show(); */

// if there is only one parameter.
/* const showNumber = x => console.log(x);
showNumber(49); */